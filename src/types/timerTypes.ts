export enum TimerState {
  IDLE = 'idle',
  RUNNING = 'running',
  PAUSED = 'paused',
  COMPLETED = 'completed',
}

export enum TimerMode {
  FOCUS = "focus",
  SHORT_BREAK = "shortBreak",
  LONG_BREAK = "longBreak",
}
