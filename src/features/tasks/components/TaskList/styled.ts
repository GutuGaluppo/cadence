import { Box } from "@mui/material";
import styled from "styled-components";

export const ListWrapper = styled(Box)({
  width: "100%",
  maxWidth: 400,
  margin: "0 auto",
  marginTop: "32px",
});

export const AddTaskRow = styled(Box)({
  display: "flex",
  gap: "8px",
  marginBottom: "24px",
});
